public enum ItemSortType
{
    None, //默认拾取顺序
    Quantity, //按数量
    Rarity, //按稀有度
    Type, //按类型
    Name //按名称
}

public enum SortOrder
{
    Ascending, //升序
    Descending //降序
}

public enum ItemRarity
{
    Common,
    Valuable,
    Rare,
} 